{
  "name": "autocontentposter-pro",
  "version": "2.0.0",
  "main": "server.js",
  "scripts": {
  "start": "node server.js",
  "dev": "npx kill-port 4000 4001 && concurrently \"nodemon server.js\" \"browser-sync start --proxy http://localhost:4001 --files 'public/**/*.*' --no-notify --no-open\"",
  "dev:4000": "npx kill-port 4000 && concurrently \"nodemon server.js\" \"browser-sync start --proxy http://localhost:4000 --files 'public/**/*.*' --no-notify --no-open\""
},
  "dependencies": {
    "axios": "^1.8.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "replicate": "^1.4.0"
  },
  "devDependencies": {
    "browser-sync": "^3.0.2",
    "concurrently": "^9.1.2",
    "kill-port": "^2.0.1",
    "nodemon": "^3.1.0"
  }
}
